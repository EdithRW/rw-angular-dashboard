export const environment = {
  // The following environment variables are set by the build process. See the README for more information
  production: false,
  base_url : 'http://localhost:8000/api'
}
